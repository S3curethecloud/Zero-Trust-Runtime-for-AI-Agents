# Example: Python agent client

This is a tiny example showing how an agent would request a JIT token from AIB.

```bash
python agent_request_token.py
```

You must have `docker compose up` running at repo root first.
