#!/usr/bin/env bash
set -euo pipefail

# Build an OPA bundle from policy directory (requires opa CLI installed).
# Docs: https://www.openpolicyagent.org/docs/management-bundles
#
# Example:
#   opa build -b services/opa/policy -o dist/aib-bundle.tar.gz
#
echo "TODO: install opa CLI and run: opa build -b services/opa/policy -o dist/aib-bundle.tar.gz"
